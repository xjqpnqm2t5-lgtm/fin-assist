fin-assist.uz — AI Finance Assistant (deployable prototype)

Quick start (local):
1. Install Node.js (LTS)
2. Copy the project and run:
   npm install
   cd client
   npm install
   cd ..
   npm run dev
3. Open http://localhost:3000
   Login: admin
   Password: password123

Deployment notes:
- Vercel: upload the repo to GitHub and import to Vercel.
- Provide OPENAI_API_KEY and JWT_SECRET in environment settings.
- Default currency set to UZS on UI.

Domain:
- You selected fin-assist.uz as the site name (configure DNS after deployment).
